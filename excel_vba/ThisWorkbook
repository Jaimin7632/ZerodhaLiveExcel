Private Sub Workbook_Open()
    ' Auto start live feed when Excel opens
    On Error Resume Next
    StartLiveFeed
End Sub

Private Sub Workbook_BeforeClose(Cancel As Boolean)
    ' Stop timer cleanly when Excel closes
    On Error Resume Next
    StopLiveFeed
End Sub